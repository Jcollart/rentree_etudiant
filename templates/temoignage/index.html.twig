{% extends 'base.html.twig' %}

{% block title %}Temoignage index{% endblock %}

{% block body %}
    <h1>Temoignage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Noms_temoignage</th>
                <th>Photo_temoignage</th>
                <th>Temoignage</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for temoignage in temoignages %}
            <tr>
                <td>{{ temoignage.id }}</td>
                <td>{{ temoignage.nomsTemoignage }}</td>
                <td>{{ temoignage.photoTemoignage }}</td>
                <td>{{ temoignage.temoignage }}</td>
                <td>
                    <a href="{{ path('temoignage_show', {'id': temoignage.id}) }}">show</a>
                    <a href="{{ path('temoignage_edit', {'id': temoignage.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('temoignage_new') }}">Create new</a>
{% endblock %}
