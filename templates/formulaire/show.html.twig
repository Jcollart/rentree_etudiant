{% extends 'base.html.twig' %}

{% block title %}Formulaire{% endblock %}

{% block body %}
    <h1>Formulaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formulaire.id }}</td>
            </tr>
            <tr>
                <th>Type_formulaire</th>
                <td>{{ formulaire.typeFormulaire }}</td>
            </tr>
            <tr>
                <th>Etablissement</th>
                <td>{{ formulaire.etablissement }}</td>
            </tr>
            <tr>
                <th>Equipe</th>
                <td>{{ formulaire.equipe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formulaire_index') }}">back to list</a>

    <a href="{{ path('formulaire_edit', {'id': formulaire.id}) }}">edit</a>

    {{ include('formulaire/_delete_form.html.twig') }}
{% endblock %}
